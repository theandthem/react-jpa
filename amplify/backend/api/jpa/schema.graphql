type Clothes @model @auth(rules: [{allow: public}]) {
  id: ID!
  type: String! @index(name: "clothesByDate", queryField: "clothesByDate", sortKeyFields: ["createdAt"])
  notes: String
  date: AWSDate!
}
 